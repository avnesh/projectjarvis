{
  "name": "jarvis-ai-fullstack",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "cd server && npm start",
    "railway:build": "cd client && npm install && npm run build && cd ../server && npm install",
    "railway:start": "cd server && npm start"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}